# Database Configuration
DB_HOST=localhost
DB_PORT=1433
DB_NAME=GmailCriteria
DB_USER=sa
DB_PASSWORD=MyPass@word123

# Feature Flags
USE_SQL_DATABASE=false

# Server Configuration
PORT=5000
NODE_ENV=development

# API Authentication (for CLI/testing)
# Generate with: node -e "console.log(require('crypto').randomUUID())"
API_KEY=your-api-key-here

# Default user email when using API key without X-Test-User header
DEFAULT_USER=api@localhost
